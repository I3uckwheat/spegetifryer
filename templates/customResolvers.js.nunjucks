const {
  GraphQLBoolean,
  GraphQLFloat,
  GraphQLInt,
  GraphQLList,
  GraphQLObjectType,
  GraphQLString,
} = require('graphql');

module.exports = {
  // hello: {
  //  description: 'test',
  //  type: GraphQLString,
  //  resolve: () => 'world'
  // } 
}